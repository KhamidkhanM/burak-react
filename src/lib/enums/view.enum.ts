// what kind of entity a view log record is tracking a view of
export enum ViewGroup {
    MEMBER = "MEMBER", // a view of a member's profile (e.g. a restaurant page)
    PRODUCT = "PRODUCT", // a view of a product
}
